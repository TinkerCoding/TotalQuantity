import React from "react";
import count from "./Quantity";


const TotalQuantity = (value) => {
};


const numbers = [count]

const sumArrayOfNumbers =(numbersArray) => {
   let total = 0;

   for (
     let i = 0;
     i < numbersArray.length;
     i += 1
   ) {
     total += numbersArray[i];
   }

   return total;


}


TotalQuantity(sumArrayOfNumbers(numbers));

export default TotalQuantity;
